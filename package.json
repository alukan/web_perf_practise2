{
  "devDependencies": {
    "lighthouse": "^11.7.0",
    "rollup-plugin-visualizer": "^5.12.0",
    "vite": "^5.2.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview --port 3000",
    "audit": "lighthouse http://localhost:3000 --only-categories=best-practices --only-categories=performance --view",
    "stop": "kill $!"
  }
}
